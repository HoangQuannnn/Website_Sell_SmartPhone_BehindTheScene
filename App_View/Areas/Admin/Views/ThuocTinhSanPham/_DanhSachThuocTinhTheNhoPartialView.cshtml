@using System.Web
@model List<App_Data.ViewModels.ThuocTinh.ThuocTinhViewModel>
<style>
    thead {
        background: #0eabe7;
    }

        thead > tr > th {
            color: white !important;
        }
</style>
<div style="padding:20px">
    <div class="text-right mb-3">
        <button type="button" class="btn btn-success" onclick="ThemTheNho()">
            <i class="fas fa-plus"></i> Thêm Thẻ Nhớ
        </button>
    </div>
    <table id="data-table" class="table table-bordered table-hover">
        <thead>
            <tr>
                <th class="text-center">STT</th>
                <th class="text-center">Mã</th>
                <th class="text-center">Loại Thẻ Nhớ</th>
                <th class="text-center">Dung Lượng</th>
                <th class="text-center">Trạng thái</th>
                <th class="text-center">Biến thể liên quan</th>
                <th class="text-center">###</th>
            </tr>
        </thead>
        <tbody>
            @{
                var STT = 1;
            }
            @foreach (var item in Model)
            {
                <tr>
                    <td class="text-center">@(STT++)</td>
                    <td class="text-center">@item.Ma!.ToUpper()</td>
                    <td class="text-center">@item.LoaiTheNho!.ToUpper()</td>
                    <td class="text-center">@item.DungLuong!.ToUpper()</td>
                    <td class="text-center">@item.TrangThai</td>
                    <td class="text-center">@item.SoBienTheDangDung</td>
                    <td class="text-center">
                        <div class="btn-group">
                            <button type="button" class="btn btn-kio" onclick="Edit('@item.Id', '@Html.Raw(HttpUtility.JavaScriptStringEncode(item.LoaiTheNho))', '@item.SoBienTheDangDung')
">
                                <i class="far fa-edit"></i>
                            </button>
                            <button type="button" class="btn" style="background: red; color: #fff" onclick="Delete('@item.Id','@Html.Raw(HttpUtility.JavaScriptStringEncode(item.LoaiTheNho))','@item.SoBienTheDangDung')">
                                <i class="anticon anticon-delete"></i>
                            </button>
                        </div>

                    </td>
                </tr>
            }
        </tbody>
    </table>

</div>
<script>
    function DrawTheNho() {
        $.ajax({
            url: '/admin/thuoctinhsanpham/LoadPartialViewDanhSachTheNho',
            type: 'GET',
            success: function (result) {
                content.empty().append(result);
            },
            error: function (xhr, status, error) {
                console.log(xhr.responseText);
                console.log(status);
                console.log(error);
                console.log(xhr);
            }
        });
    }
    $('#data-table').Datatable();
    function Delete(id, name, soBienTheDangDung) {
        if (soBienTheDangDung > 0) {
            Swal.fire({
                title: 'Xóa thất bại',
                text: `"${name}" đang được dùng cho ${soBienTheDangDung} biến thể.`,
                icon: 'info',
                confirmButtonText: 'OK'
            });
        } else {
            Swal.fire({
                title: 'Xác nhận',
                text: `Bạn có chắc chắn muốn xóa thẻ nhớ "${name}" không?`,
                icon: 'question',
                showCancelButton: true,
                confirmButtonText: 'OK',
                cancelButtonText: 'Hủy',
            }).then((result) => {
                if (result.isConfirmed) {

                    var data = {
                        idTheNho: id,
                    };
                    $.ajax({
                        url: '/admin/thuoctinhsanpham/DeleteTheNho',
                        type: 'GET',
                        data: data,
                        success: function (result) {
                            if (result) {
                                DrawTheNho();
                                Swal.fire(
                                    'Thông báo',
                                    'Đã xóa thành công',
                                    'success'
                                );
                            }
                        },
                        error: function (xhr, status, error) {
                            console.log(xhr.responseText);
                            console.log(status);
                            console.log(error);
                            console.log(xhr);
                        }
                    });
                }
            });

        }
    }

    function Edit(id, maTheNho, loaiTheNho, dungLuong, soBienTheDangDung) {
        Swal.fire({
            title: 'Chỉnh sửa thẻ nhớ',
            input: 'text',
            inputValue: loaiTheNho,
            inputPlaceholder: 'Nhập giá trị',
            showCancelButton: true,
            confirmButtonText: 'Lưu',
            cancelButtonText: 'Hủy',
        }).then((result) => {
            if (result.isConfirmed) {
                const enteredValue = result.value;

                if (enteredValue !== '') {

                    Swal.fire({
                        title: 'Xác nhận',
                        text: `Bạn có chắc chắn muốn thay đổi thẻ nhớ, việc thay đổi thẻ nhớ sẽ làm cho thẻ nhớ của ${soBienTheDangDung} biến thể thay đổi theo?`,
                        icon: 'question',
                        showCancelButton: true,
                        confirmButtonText: 'OK',
                        cancelButtonText: 'Hủy',
                    }).then((result) => {
                        if (result.isConfirmed) {

                            var data = {
                                IdTheNho: id,
                                maTheNho: maTheNho,
                                loaiTheNho: enteredValue,
                                dungLuong: enteredValue
                            };
                            $.ajax({
                                url: '/admin/thuoctinhsanpham/EditTheNho',
                                type: 'POST',
                                data: JSON.stringify(data),
                                contentType: 'application/json',
                                success: function (result) {
                                    if (result) {
                                        DrawTheNho();
                                        Swal.fire(
                                            'Thông báo!',
                                            'Cập nhật thành công!',
                                            'success'
                                        );
                                    } else {
                                        Swal.fire(
                                            'Thông báo',
                                            'Cập nhật thất bại!',
                                            'info'
                                        );
                                    }
                                },
                                error: function (xhr, status, error) {
                                    console.log(xhr.responseText);
                                    console.log(status);
                                    console.log(error);
                                    console.log(xhr);
                                }
                            });
                        }
                    });
                } else {
                    Swal.fire(
                        'Lỗi!',
                        'Vui lòng nhập giá trị.',
                        'error'
                    );
                }
            }
        });
    }
    function ThemTheNho() {
        Swal.fire({
            title: 'Thêm Thẻ Nhớ mới',
            html:
                '<input id="maTheNho" class="swal2-input" placeholder="Mã Thẻ Nhớ">' +
                '<input id="loaiTheNho" class="swal2-input" placeholder="Loại Thẻ Nhớ">' +
                '<input id="dungLuong" class="swal2-input" placeholder="Dung Lượng">' +
                '<div class="form-group">' +
                '    <label for="trangThai">Trạng thái:</label>' +
                '    <select class="form-control" id="trangThai">' +
                '        <option value="0">Hoạt động</option>' +
                '        <option value="1">Không hoạt động</option>' +
                '    </select>' +
                '</div>',
            showCancelButton: true,
            confirmButtonText: 'Thêm',
            cancelButtonText: 'Hủy',
            preConfirm: () => {
                const maTheNho = document.getElementById('maTheNho').value;
                const loaiTheNho = document.getElementById('loaiTheNho').value;
                const dungLuong = document.getElementById('dungLuong').value;
                const trangThai = document.getElementById('trangThai').value;
                return { loaiTheNho: loaiTheNho,dungLuong: dungLuong, trangThai: parseInt(trangThai) }; // Chuyển đổi giá trị sang kiểu số nguyên
            }
        }).then((result) => {
            if (result.isConfirmed) {
                const data = result.value;
                if (data.loaiTheNho !== '' && data.trangThai !== '' && data.maTheNho !== '' && data.dungLuong != '') {
                    // Gửi dữ liệu thông qua AJAX để thêm Chip mới vào cơ sở dữ liệu
                    $.ajax({
                        url: '/admin/thuoctinhsanpham/CreateTheNho',
                        type: 'POST',
                        data: JSON.stringify(data),
                        contentType: 'application/json',
                        success: function (result) {
                            if (result) {
                                DrawTheNho();
                                Swal.fire(
                                    'Thông báo!',
                                    'Thêm Thẻ Nhớ mới thành công!',
                                    'success'
                                );
                            } else {
                                Swal.fire(
                                    'Thông báo',
                                    'Thêm Thẻ Nhớ mới thất bại!',
                                    'info'
                                );
                            }
                        },
                        error: function (xhr, status, error) {
                            console.log(xhr.responseText);
                            console.log(status);
                            console.log(error);
                            console.log(xhr);
                        }
                    });
                } else {
                    Swal.fire(
                        'Lỗi!',
                        'Vui lòng nhập đầy đủ thông tin.',
                        'error'
                    );
                }
            }
        });
    }
</script>
<script>
    $('#data-table').Datatable();
</script>
<script src="~/Admin/vendors/datatables/jquery.dataTables.min.js" defer></script>
<script src="~/Admin/vendors/datatables/dataTables.bootstrap.min.js" defer></script>
<script src="~/Admin/js/pages/datatables.js" defer></script>